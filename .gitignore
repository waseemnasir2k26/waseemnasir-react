node_modules
.next
out
.vercel
.env*.local
*.log
.DS_Store

.vercel

# critic scratch (screenshots, capture scripts)
.critic/

# Next.js incremental TS build cache — dirties the tree on every build
tsconfig.tsbuildinfo
